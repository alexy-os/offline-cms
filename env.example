# CMS (Vite) configuration
#
# This is the GraphQL endpoint the admin UI talks to.
# In this monorepo, it should point to apps/api.
VITE_GRAPHQL_ENDPOINT=http://localhost:5001/graphql

# Optional: the port used by the container start command (defaults to Vite's 5173)
PORT=5173